import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'proyecto.prueba.v1',
  appName: 'proyecto-prueba-1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
